name: Build Docker Containers

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build-alpine:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Alpine container
        uses: docker/build-push-action@v5
        with:
          context: ./alpine
          file: ./alpine/Containerfile
          tags: |
            ghcr.io/${{ github.repository_owner }}/bare-metal-dev-alpine:latest
            ghcr.io/${{ github.repository_owner }}/bare-metal-dev-alpine:${{ github.sha }}
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-arch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Arch container
        uses: docker/build-push-action@v5
        with:
          context: ./arch
          file: ./arch/Containerfile
          tags: |
            ghcr.io/${{ github.repository_owner }}/bare-metal-dev-arch:latest
            ghcr.io/${{ github.repository_owner }}/bare-metal-dev-arch:${{ github.sha }}
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Ubuntu container
        uses: docker/build-push-action@v5
        with:
          context: ./ubuntu
          file: ./ubuntu/Containerfile
          tags: |
            ghcr.io/${{ github.repository_owner }}/bare-metal-dev-ubuntu:latest
            ghcr.io/${{ github.repository_owner }}/bare-metal-dev-ubuntu:${{ github.sha }}
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
